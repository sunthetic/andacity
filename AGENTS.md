# Project Instructions

## Database Commands

Before running any Postgres, migration, seed, or database validation command for Andacity, read `/home/alden/a/andacity/.env.development` and use `DATABASE_URL` from that file.

Do not assume or hardcode a localhost Postgres connection string unless the user explicitly instructs you to do so.
